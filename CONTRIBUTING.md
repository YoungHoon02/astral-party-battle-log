# 기여 안내

PR을 환영합니다. 이 문서는 기여자가 기능을 추가하거나 수정할 때 반드시 지켜야 할
정보 수집 범위, 개인정보 보호 기준과 검증 절차를 설명합니다.

기술적인 배경(왜 Harmony를 못 쓰는지, protobuf 함정, 프로토콜에서 밟은 지뢰 등)은
[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)에 따로 정리해 뒀습니다. **디코더를
건드리기 전에 반드시 읽어 주세요.** 모르고 고치면 조용히 틀립니다.

---

## 1. 허용목록은 정책이 아니라 구조다

`Log/Opcodes.cs`의 `Op.Allowed` **밖에 있는 메시지는 본문을 파싱하지 않습니다.**
출력에서 가리는 게 아니라 디코더가 존재하지 않는 것이고, 그래서 코드를 읽으면
안전이 증명됩니다.

> "일단 다 덤프하고 나중에 거르자"로 바꾸지 마세요. 실수 한 줄이 PvP 치트가 됩니다.

새 메시지를 읽고 싶다면:

1. 그 메시지가 **화면에 공개되는 정보**만 담고 있는지 확인합니다
2. `Op.Allowed`에 추가하고, 디코더에서 **필요한 필드만** 읽습니다
3. PR 본문에 "이 메시지가 왜 공개 정보인지"를 한 줄로 적어 주세요

본문 없이 **도착 사실만** 필요하면(예: 판 시작을 알리는 `MatchSuccessS2C`)
`Op.GameEntry`처럼 따로 두고 헤더의 cmdID와 ERR만 봅니다. 이 경우에는 본문 디코더를
만들거나 본문 필드에 접근하지 마세요.

### 카드에 그은 선

카드 기록은 이 모드의 핵심 기능이라 넣었지만, 경계를 **메시지 종류**로 긋습니다.

| | 디코딩 | 근거 |
| --- | --- | --- |
| `UseEffectCardS2C`(5056) | **한다** | 보드에서 공개적으로 쓴 카드. 값이 설정 id다 |
| 〃 의 스킬 분기 | **한다** | 카드가 아니라 스킬이다 |
| `BattleUseCardS2C`(5036) | "냈다"만 | 값이 카드 uid라 종류를 알 수 없다 |
| `BattleRole.useCards`(필드 5) | **안 읽는다** | 역시 uid다 (packed sfixed32) |
| **`HeroAttrEffect.Card`(필드 9)** | **안 한다** | **손패 변경.** 마스킹된 `-1`이 오는 곳 |
| `ShopBuyS2C`(5030) | **안 한다** | 이름과 달리 `BuyCards`/`Cards`를 담은 카드 메시지 |

즉 **"카드를 냈다"는 읽고, "손패가 바뀌었다"는 읽지 않습니다.** 손패를 읽는 코드
경로는 이 리포에 존재하지 않으며, 앞으로도 그래야 합니다.

`LogCards` 설정으로 카드 기록을 끌 수 있지만, **손패를 읽지 않는 성질은 설정과
무관하게 유지**됩니다. 설정으로 가리는 것과 코드가 없는 것은 다릅니다.

**감사 방법** — 카드 id를 이름으로 바꾸는 지점이 하나뿐인지 확인합니다:

```bash
grep -rn 'Lookup("card"' --include='*.cs' Log
```

`CardLabel` 한 군데만 나와야 합니다. 늘어났다면 경계가 움직인 것입니다.

## 2. 계정 식별자와 닉네임을 로그·캐시에 쓰지 않는다

서버가 보내는 `Player.Id`는 매치용 슬롯이 아니라 **영구 계정 uid**이고, `Player.Nick`은
**계정 닉네임**입니다. 로그를 남에게 보여줄 때 같이 나가면 안 되므로 본문에는 캐릭터
이름만 씁니다 (`LogPlayerIds`를 켜면 진단용으로 참가자 줄에만 uid가 붙습니다).

- **`Nick`은 읽지 않습니다.** `Log/Roster.cs`에는 닉네임을 파싱하는 코드가 없습니다.
  이름표에서 캐릭터를 못 찾으면 `2P`/`몹` 같은 자리표시자를 씁니다. 표시명이 비어
  보인다고 닉네임을 폴백으로 되살리지 마세요 — 손패와 마찬가지로 설정으로 가리는 것과
  코드가 없는 것은 다릅니다.
- **재접속 캐시(`roster-cache.tsv`)에는 이름표에서 찾은 캐릭터·몹 이름만 저장합니다.**
  자리표시자도 저장하지 않습니다. 헤더 버전이 맞지 않는 예전 파일(v2 이하)은 닉네임이
  들어 있을 수 있어 읽지 않고 지웁니다. 캐시에 넣는 값을 늘리면 헤더 버전을 올려 주세요.
- **캐시 파일은 공유하지 않습니다.** 키는 uid의 SHA-256 앞 8바이트지만 솔트가 없고 uid
  범위가 좁아 후보를 차례로 해시하면 되돌릴 수 있습니다. 이슈 첨부나 로그 공유를
  안내할 때 이 파일은 대상에서 빼 주세요.

**감사 방법** — `Roster`가 `Player`의 문자열 필드를 읽지 않는지 확인합니다:

```bash
grep -n 'TryReadString' Log/Roster.cs
```

아무것도 나오지 않아야 합니다.

## 3. 게임에서 나온 것은 커밋하지 않는다

`.gitignore`에 막아 뒀습니다. 우회해서 추가하지 마세요.

| | 왜 |
| --- | --- |
| `ref/*.dll` | 추출한 게임 어셈블리 |
| `names.tsv` | 게임 텍스트 + 한글패치 번역문 |
| `docs/types-all.txt` | 게임 어셈블리 전체 타입 목록 |

진단 산출물(`replay-*.jsonl` 재생 기록, `[probe]`·`[candidates]` 로그)도 커밋하거나 공유하지 마세요. 전투 텍스트와
계정 식별자는 넣지 않지만 게임 진행 시각이 담기고, 경로 가림(`ScreenRecorder.Mask`)은 비ASCII·6자리 이상 숫자·
알려진 카메라 접미사만 가리는 휴리스틱이라 다른 모양의 식별 정보가 남을 수 있습니다.

이 리포의 MIT 라이선스는 **여기 있는 코드에만** 적용되며, Astral Party의 자산·
프로토콜·거기서 파생된 데이터를 재배포할 권리를 주지 않습니다. 그것들은 각
권리자(게임사, 그리고 한국어 문자열의 경우 한글패치 번역자)의 것입니다.

`names.tsv`를 커밋하지 않아도 되는 이유는 **플러그인이 각자의 게임에서 직접
만들기 때문**입니다 (`Log/NameHarvest.cs`). 구조를 바꿀 때 이 성질을 깨지 마세요.

## 4. 크래시가 확정된 것들

같은 게임의 다른 모드에서 실측으로 확인된 것입니다. Il2CppInterop의
"Class::Init signatures have been exhausted" 버그 때문에 **IL2CPP 쪽에 새
델리게이트/타입을 등록하는 모든 행위가 AccessViolationException을 냅니다.**

- `ClassInjector.RegisterTypeInIl2Cpp<T>()` — 금지
- IL2CPP 쪽 C# 이벤트에 `+=` 구독 — 금지
- 코루틴 `WrapToIl2Cpp()` — 금지

따라서 `OnGUI` MonoBehaviour를 심는 IMGUI 오버레이는 만들 수 없고, `Update`도
만들 수 없습니다. 대안은 [ARCHITECTURE.md](docs/ARCHITECTURE.md)에 있습니다.

또한 **게임 로직에는 Harmony를 걸 수 없습니다** — 게임 코드가 HybridCLR
핫업데이트 어셈블리라 interop stub이 없고, 인터프리터의 `methodPointer`가
시그니처별 공유 브릿지라 무관한 메서드까지 잡습니다.

## 5. 디코더를 고칠 땐 먼저 대조한다

손으로 짠 protobuf 디코더는 **틀려도 예외가 안 납니다.** 값이 0으로 나오거나
필드가 통째로 건너뛰어지고, 로그에는 "그냥 그 정보가 없는 것"처럼 보입니다.
실제로 이 방식으로 잡은 버그가 다섯 건입니다 (sfixed / map / Noop / packed / UseSkill).

```bash
python tools/dump_tags.py <디컴파일루트> HeroBuffChangeS2C
```

디컴파일된 원본에서 **실제 태그 바이트**를 뽑아 줍니다.

> **`FieldNumber` 선언을 근거로 삼지 마세요.** 그건 필드 *번호*만 알려줍니다.
> 실제로 틀리는 곳은 **wire type**과 **모양**(packed / map / message)이고,
> 그건 직렬화 코드에만 나옵니다.

읽는 법과 흔한 함정은 [ARCHITECTURE.md](docs/ARCHITECTURE.md)의 "protobuf의 함정"과
"프로토콜에서 밟은 지뢰"를 보세요.

## 6. PvP 정보 우위에 대해

이 플러그인은 네트워크 계층에서 읽으므로 **게임 애니메이션보다 먼저 결과를
압니다.** 이건 설계상 피하기 어렵고, README에 이용자에게 고지하고 있습니다.

**이 격차를 더 벌리는 기능은 받지 않습니다.** 예를 들어 아직 공개되지 않은 정보를
미리 보여주거나, 상대의 선택을 예측해 알려주는 기능은 목적 밖입니다. 이 모드는
**이미 일어난 일을 읽기 좋게 적는 것**까지입니다.

## 7. 게임 없이 도는 검증 하네스

`tools/harness`는 플러그인 소스를 **그대로 링크해** 콘솔에서 돌리는 검증 도구입니다.
합성 프레임을 넣어 디코더 출력을 보고, 표시 예약 큐는 시계를 직접 돌려 확인합니다.
검증 대상과 배포 코드가 갈라질 수 없다는 점이 이 방식의 목적입니다.

```bash
dotnet run --project tools/harness/harness.csproj
```

`BepInEx.Core.dll`만 참조하므로 게임을 켤 필요가 없습니다. 경로가 다르면
`-p:BepInExDir=<내 BepInEx 폴더>`로 넘기세요. 실패가 있으면 종료 코드가 실패 수가 됩니다.

- `A`~`I`는 눈으로 보는 출력 예시입니다 (스킬 메아리, HP 필터, 카드 제출 중복, 종류·그룹 태깅).
- `J`~`K`, `P`, `H1`, `F1`~`F3`은 자동 판정입니다 — 카드 기록 끔, 화면 동기화 신호 대상,
  순차 재생, 프레임 헤더, 파일 기록 스레드.

**타이밍·동기화나 디코더를 고치면 여기에 항목을 추가하세요.** 실게임 검증을 대신하지는
못합니다. 표시 시점의 실제 정확도는 게임에서만 확인할 수 있습니다
([OVERLAY-TIMING.md](docs/OVERLAY-TIMING.md)).

## 8. 릴리스 (메인테이너)

빌드와 패키징은 로컬에서 합니다. 플러그인 본체는 게임에서 나온 interop 어셈블리를
참조하므로(§3) CI에서 빌드할 수 없습니다.

릴리스 본문의 원본은 **README의 버전 섹션**입니다. 커밋 메시지는 무엇을 어떻게
고쳤는지(개발자 관점), 릴리스 노트는 이용자에게 무엇이 달라졌는지(이용자 관점)를
적는 다른 층위라서 커밋 로그를 노트로 기계 변환하지 않습니다.

1. `AstralPartyBattleLog.csproj`의 `Version`과 `Plugin.cs`의 `BepInPlugin` 버전을 함께 올립니다
2. README에 `## v0.2.0` 섹션을 추가하고 달라진 점을 이용자 관점으로 적습니다
3. **게임에서 실제로 돌려 확인합니다.** 여기가 유일한 관문입니다
4. 게시합니다

```powershell
.\tools\publish-release.ps1
```

스크립트는 브랜치·작업 트리·태그 중복을 확인하고, README에서 해당 버전 섹션을 뽑아
`build-release.ps1`로 ZIP을 만든 다음, 노트 전문과 패키지 경로를 보여 주고 확인을 받아
태그를 푸시하고 릴리스를 게시합니다.

- README에 그 버전 섹션이 없으면 **중단하면서 지난 태그 이후의 커밋을 보여 줍니다.**
  노트를 쓸 재료입니다.
- 원격 `main`과 HEAD가 다르면 중단합니다. 릴리스가 원격에 없는 커밋을 가리키면 받은
  사람이 소스를 대조할 수 없습니다. `git push origin main`을 먼저 하세요.
- 게시가 실패하면 **릴리스가 없다고 확인된 경우에만** 태그를 되돌립니다. 그대로 다시
  실행하면 됩니다. 릴리스가 남아 있거나 인증·네트워크 때문에 상태를 확인하지 못하면
  태그를 그대로 두고, 이어서 할 명령을 알려 줍니다.
- `-Draft`는 노트에 스크린샷을 붙이거나 공개 시점을 따로 잡을 때만 씁니다. 평소에는
  게시 전 확인 단계가 같은 일을 합니다.
- `gh` CLI가 필요합니다 — `winget install --id GitHub.cli` 뒤 `gh auth login`.

---

## PR 체크리스트

- [ ] `dotnet build -c Release`가 **경고 0개**로 통과하는가
- [ ] 새 메시지를 읽었다면 `Op.Allowed` 추가 근거를 PR에 적었는가
- [ ] `grep -rn 'Lookup("card"' --include='*.cs' Log`가 여전히 한 줄인가
- [ ] `grep -n 'TryReadString' Log/Roster.cs`가 여전히 비어 있는가
- [ ] 게임 자산(`names.tsv`, `*.dll`, 타입 목록)이 커밋에 섞이지 않았는가
- [ ] 디코더를 고쳤다면 `tools/dump_tags.py`로 대조했는가
- [ ] `dotnet run --project tools/harness/harness.csproj`가 실패 0으로 끝나는가
- [ ] 단순한 흐름이나 표준 코드에 설명용 주석을 추가하지 않았는가
- [ ] 복잡한 프로토콜 처리나 직관적이지 않은 안전 규칙에는 **왜 필요한지**를
      설명했는가
- [ ] 아키텍처, 조사 과정과 긴 배경 설명은 코드 주석 대신 `docs/` 문서에 반영했는가

## 코드 스타일

특별한 도구는 없습니다. 주변 코드에 맞춰 주세요.

- 반복문, 조건문, 데이터 변환처럼 코드만으로 이해할 수 있는 내용에는 주석을 쓰지 않습니다
- 복잡한 알고리즘, 프로토콜 함정, 개인정보 보호처럼 실수 가능성이 큰 규칙에만
  **무엇을 하는지보다 왜 필요한지**를 한국어로 적습니다
- 실측으로 알아낸 것은 숫자와 함께 남깁니다 (`실측 3회: 9+2=11, 8+2=10, 5+5=10`)
- 되돌리기 쉬운 결정과 어려운 결정을 구분해서, 어려운 쪽에 근거를 더 답니다
