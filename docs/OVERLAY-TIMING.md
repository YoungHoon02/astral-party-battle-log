# 오버레이 연출 동기화

서버는 봇 행동을 화면 재생보다 먼저 보낼 수 있다. 오버레이는 받은 순서를 유지하면서
연출 길이를 누적해 화면 표시 시점을 추정한다. 파일 로그는 수신 시각에 기록한다.

> **검증 상태:** 화면 신호 연결(`Overlay.ScreenSignals`, 기본 false)은 봇전 세 판(측정 4
> 여섯째~여덟째 판)에서 캐릭터 주사위 65건과 PK 38건을 화면보다 먼저 띄우지 않았다. 반격은 기록
> 재생과 하네스로만 확인했고, **PvP·배속은 확인하지 않았다.** 아래 측정 1~3은 기존 연출 길이 모델을
> 만들 때의 관측 기록이다.

## 원칙

**늦추는 것은 오버레이 출력뿐이다.**

```
패킷 수신 → 프레임 재조립 → 즉시 해석 → 로거 상태 즉시 갱신 → 줄 생성
    ├─ battle-log.txt : 지금 기록 (시각·순서 그대로)
    └─ OverlaySchedule : 예약 → 연출 시점에 LogOverlay로
```

해석이나 상태 갱신을 늦추면 안 되는 이유:

- 턴 주인·원인 문맥(`_turnOwner`, `_said*`)이 다음 패킷과 뒤섞인다
- 골드 송금 짝 맞추기(600ms 보류)가 깨진다
- 소켓 스레드가 기다리게 된다

600ms 골드 보류는 **문장을 만들기 위한 것**이고 표시 예약은 **화면에 내보내기 위한
것**이다. 섞지 않는다 — 보류됐던 줄은 원래 패킷의 종류·그룹을 들고 예약 큐에 들어간다.

## 구성

| 위치 | 역할 |
| --- | --- |
| `Log/LineKind.cs` | 사건 종류 |
| `Log/BattleLogger.cs` | 패킷마다 종류·그룹을 정하고 줄에 붙여 넘긴다 |
| `UI/OverlaySchedule.cs` | 사건을 순서대로 예약하고 동기화 지점을 반영한다. Unity에 의존하지 않는다 |
| `UI/FramePump.cs` | 매 프레임 시계·배속을 넘기고 기한이 된 줄을 꺼낸다 |
| `UI/LogOverlay.cs` | 기존 그대로 그린다 |

소켓 스레드는 `OverlaySchedule.Line/Advance/Page/Reset/Sync`로 넣기만 한다. Unity 객체에 손대지
않고, 기다리지 않고, 디스크에 쓰지 않는다.

### 파일 기록도 소켓 스레드에서 뺐다

전에는 `BattleLogger`가 줄마다 소켓 스레드에서 `File.AppendAllText`를 불렀다. 디스크가
잠깐 막히면 그만큼 다음 패킷 처리가 밀린다(AnimSpeedMod에서 설정 파일 쓰기가 최악 46ms를
막은 기록이 있다). 이제 `Log/LogFileWriter.cs`의 전용 스레드가 쓴다.

- **시각은 넣을 때 찍는다.** 해석 시점의 `HH:mm:ss.fff`라 파일 내용·순서·시각은 전과 같다.
- 새 판의 파일 비우기도 같은 큐를 타서 줄과 순서가 섞이지 않는다.
- 큐(2만 줄)가 차면 그 줄은 버리고 경고를 한 번 남긴다 — 소켓 스레드를 막지 않는 쪽을 골랐다.
- 플러그인 언로드와 프로세스 종료(`AppDomain.ProcessExit`, 순수 .NET 이벤트)에서 남은 줄을
  최대 1초 기다려 쓴다.

## 시계

**프레임 시계**를 쓴다. 매 프레임 `Time.unscaledDeltaTime`을 더하되
`Time.maximumDeltaTime`으로 자른다.

- 게임 연출은 Unity가 잘라낸 `deltaTime`으로 진행한다. 3초 끊김이 있어도 연출은
  0.333초만 나아간다. 실시간 시계를 쓰면 끊김 뒤에 로그가 연출보다 먼저 한꺼번에 풀린다.
- 창이 멈춰 프레임이 안 돌면 시계도 멈춘다. 연출도 멈추므로 맞다.
- Unity 값을 못 읽으면 `Stopwatch`로 대신 흐른다. 시계가 멈추면 로그가 영영 안 나오므로
  배속 연동 실패가 전투 로그 실패로 번지지 않게 한다.

수신 시각은 소켓 스레드가 이 시계를 `Volatile.Read`로 읽어 찍는다. 해상도는 한 프레임이다.

## 배속

**`Time.timeScale`을 읽는다.** AnimSpeedMod(`AstralPartyAnimSpeedMod/Plugin.cs`
`OnFrameTick`)가 배속을 바로 이 값에 쓴다:

```csharp
if (SpeedsDiffer(Time.timeScale, target)) Time.timeScale = target;
```

그래서 다른 모드의 내부 필드를 리플렉션할 필요가 없다. 모드가 없으면 1이고, 배속 제외
씬에서는 AnimSpeedMod가 1로 되돌린다. **게임 자체의 방 속도 설정은 이 값에 드러나지
않는다** — [알려진 한계](#알려진-한계).

- `Time.deltaTime`에서 역산하지 않는다 — 프레임 저하와 배속을 구분할 수 없다.
- 유한하고 0.25~10 사이가 아니면 1로 본다. 이 범위는 측정값이 아니라 안전장치다
  (AnimSpeedMod 배속 목록은 1·1.5·2·3·5). 게임 자체 연출도 `timeScale`을 바꿀 수 있다.
- **줄을 넣을 때의 배속**으로 연출 길이를 환산한다. 기다리는 도중 배속이 바뀌어도
  기존 예약은 다시 계산하지 않는다.

## 순차 재생

사건을 받은 순서대로 처리한다. 새 덩어리의 시작 시각은
`max(수신 시각, 이전 덩어리 종료 시각)`이고, 다음 커서는
`시작 시각 + 연출 길이 / 수신 시점 배속`이다. 줄은 덩어리 시작에 표시하되 PK 결과와 주사위는
아래 **공개 지연**만큼 늦춘다. 연속된 같은 그룹의 줄은 첫 줄의 표시 기한을 공유하며, 실제 출력은 FIFO로 한다.

**공개 지연은 덩어리가 쉬는 화면에서 시작했는지로 가른다** (`수신 시각 ≥ 커서`).

| | 밀린 덩어리 (수신 < 커서) | 쉬는 화면 (수신 ≥ 커서) |
| --- | ---: | ---: |
| PK 결과 | 1.94초 | 4.35초 |
| 주사위 | 0 | 1.60초 |

측정 4 세 판에서 결과가 화면보다 먼저 뜬 7건(주사위 5, PK 2)은 전부 쉬는 화면에서 시작한
덩어리였고, 봇 차례에서 늦게 뜬 사건은 전부 밀린 덩어리였다. 쉬는 화면에서는 줄이 연출 시작과 함께
나가므로 결과가 먼저 드러난다 — 주사위는 굴림(1.14~1.59초)이 끝나기 전, PK는 타격(수신 뒤
3.22~4.33초) 전. 값은 관측의 최댓값을 덮도록 잡았다(먼저 뜨는 쪽이 비용이 크다).

- **밀린 덩어리는 바꾸지 않는다.** 이미 2~6초 늦으므로 더 늦추면 안 된다.
- 모델은 쉰다고 봤는데 실제 화면이 밀려 있었다면 그 줄은 원래 먼저 뜨는 중이므로 늦춰도 손해가 없다.
- 동기화(결정 창 열림) 직후로만 한정하지 않은 이유: 셋째 판의 먼저 뜬 PK(−1.3초)는 동기화 뒤
  몬스터 주사위가 커서를 한 번 밀어 "동기화 직후"가 끊긴 뒤였다.
- 공개 지연은 다음 사건을 위한 연출 길이(주사위 2.70초 등)와 별개다. 커서는 그대로 민다.
- **쉬는 화면 값은 모든 덩어리와 동기화에 하한으로 건다** (`수신 + 4.35초`/`+ 1.60초`). 수신 뒤 결과가
  보이기까지의 최소 시간이기 때문이다. 두 경우를 막는다: ① 커서보다 조금 늦게 시작하는 PK가
  "밀린 덩어리" 공식으로 수신 뒤 약 1.94초에 뜨는 것, ② 본인 주사위 직후 수 ms 만에 오는 결정 창
  닫힘(5308, 9바이트) 동기화가 공개 지연을 무시하고 줄을 내보내는 것(셋째 판 본인 주사위 3건 중 2건).
  하한을 기다리는 줄 뒤의 줄도 FIFO라 함께 기다린다.
- 몬스터 주사위는 화면 신호가 없어 쉬는 화면에서 1.60초 늦춘 것이 맞는지 확인하지 못했다(늦는 쪽).

세 판의 실측에 이 규칙을 적용하면 먼저 뜨던 7건은 화면보다 0.02~1.1초 늦게 뜬다.

`Overlay.SyncMaxLagMs`는 **덩어리 시작 시각**을 `수신 시각 + 설정값` 이하로
제한한다. PK 공개 지연과 FIFO 대기 때문에 각 줄의 실제 표시 시간이 정확히 설정값
이하가 되는 것은 아니다. 대기열이 4096줄을 넘으면 오래된 줄부터 기한을 무시하고 내보낸다.

| 사건 | 1배속 연출 길이 | 코드에서의 처리 |
| --- | ---: | --- |
| 효과카드 | 2.87초 | `UseEffectCardS2C`의 카드 사용 |
| PK 카드 제출 | 2.23초 | `BattleUseCardS2C` |
| PK 전체 | 9.45초 | `BattleS2C` 결과. 결과 줄은 위 공개 지연 뒤 표시 |
| 스킬 | 1.92초 | 스킬 사용·스킬 추정 줄 |
| 판 시작 | 18.15초 | 1라운드 페이지 신호 |
| 주사위 | 2.70초 | 칸 수와 무관하다. 아래 참고 |
| 추가 이동 | 칸당 0.30초 | 받은 칸 수에 곱함. 실측 표본 없음 |

결과 줄(`UpdateHeroAttrS2C`)은 카드·스킬·PK 연출을 다시 기다리지 않도록 길이를 0으로
둔다. 땅 원인은 별도 종류로 남기고, PK 피해는 PK 결과와 같은 그룹으로 묶는다.
카드 기록을 꺼도 공개 카드 사용·PK 카드 제출은 출력 없는 예약으로 커서를 진행한다.
이 길이는 봇전 세 판의 관측 20개에 맞춘 **1배속 추정값**이다. 관측에 대한 평균 오차는
1.45초, 최대 오차는 2.77초였다. 한 판을 제외하면 일부 값의 오차가 7~9초로 커져
독립적인 검증값으로 볼 수 없다.

**주사위는 칸 수에 비례하지 않는다.** 길이를 갖는 항목이 주사위 하나뿐인 구간 일곱을 뒤에
직접 실측했다 — 1칸 1266ms · 3칸 1923ms · 5칸 1585ms · 6칸 2556ms · 8칸 2661/2345ms ·
10칸 1535ms. 회귀하면 `1539ms + 76ms×칸`에 R²=0.19로 칸 수는 설명력이 없다. 전에 여기
적혀 있던 "칸당 이동 시간만 측정 3에서 직접 읽었다"는 틀렸다 — 직접 읽었다던 그 값이
유일하게 확실히 틀린 값이었다 — 칸당 300ms는 적은 칸수에서 크게 모자랐고(1칸 300ms 대
실측 1266ms), 많은 칸수에서는 과하게 늦췄다(10칸 3000ms 대 1535ms). 2.70초는 최소제곱이
아니라 관측 일곱을 모두 덮는 하한이다(모자라면 연출보다 먼저 뜨고 남으면 조금 늦을 뿐이라
오차의 대가가 비대칭이다). 표본이 일곱이라 잠정값이다.

추가 이동의 칸당 0.30초는 네 판에서 한 건도 관측되지 않아 옛 값을 그대로 쓴다. 표본이
생기면 주사위처럼 다시 맞춰야 한다.

## 동기화

헤더의 `UPSN`이 0이 아닌 응답 중, 측정에서 화면 단계와 대응한
`TimeWastingS2C`(5308)·`BattleUseCardS2C`(5036)·`BattleChoiceS2C`(5040)만
동기화 신호로 쓴다. 신호 앞에 받은 줄은 표시 기한이 남았어도 내보내되(주사위·PK 결과는
공개 하한까지만 앞당긴다 — [순차 재생](#순차-재생)), 이후 사건을
위한 재생 커서를 그 응답의 수신 시각으로 맞춘다. 본문은 읽지 않고 허용목록도
확장하지 않는다. 다른 응답은 동기화하지 않으므로 상황에 따라 화면을 따라잡는 시점이
늦어질 수 있다.

세 신호 모두 재생 커서를 맞추는 데 쓰지만, **연출 길이를 배우는 관측으로는 검증된
"결정 창 열림"(5308, 본문 14바이트)만 쓴다.** 카드 제출 응답처럼 단계 중간에 오는
신호는 그 시점이 연출 사슬의 끝이라는 보장이 없다. 그래서 동기화 항목은 opcode와 본문
길이를 그대로 기록에 싣고, 구분은 분석 쪽(`tools/fit-timing.js`)에서 한다.

### 연출 길이 보정용 계측

결정 창은 사용자가 아니라 게임이 여는 것이라, 그 시각은 "앞선 연출 사슬이 끝난 정확한
시점"이다. 화면도 연출을 차례대로 재생하므로 도착 시각 `t_i`와 참 길이 `d_i`에 대해

```text
끝_i = max(t_i, 끝_{i-1}) + d_i,   끝_마지막 = 동기화 시각
```

이 성립한다. **이 max 때문에 "합 = elapsed - idle"로 접으면 안 된다** — 계측이 재는
idle은 모델이 자기 (틀린) 길이로 계산한 값이라 화면이 실제로 쉰 구간과 다르다. 접어서
풀면 화면이 중간에 쉰 구간에서 식이 어긋난다(3차 측정에서 같은 판의 두 구간이 +4.9초와
-9.7초로 갈렸다).

그래서 `TraceTiming`은 커서를 움직인 항목을 도착 시각과 함께 하나씩(`segitem`) 남기고,
푸는 쪽이 max를 그대로 둔 채 맞춘다. 동기화마다 남는 누계(`synclead`)는 눈으로 훑기 위한
요약이며 푸는 데 쓰지 않는다. 구간 구성을 스케줄러가 직접 남기는 이유는, 바깥에서
`show` 줄로 재구성하면 출력 없는 예약(`Advance`)이 빠지고 그룹 묶음도 흉내 내야 해서
어긋나기 때문이다. PK·주사위 공개 지연은 커서를 움직이지 않으므로 이 방법으로는
구할 수 없다.

## 초기화

각 줄은 넣을 때의 **세대**를 들고 있고, 꺼낼 때 현재 세대와 다르면 버린다. 큐를 비우는
것만으로는 이미 꺼내 둔 줄이 경쟁 상태로 남을 수 있어서다.

| 상황 | 호출 | 동작 |
| --- | --- | --- |
| 새 판(StartGame / MatchSuccess / SingleCampaign / 방 변경) | `Reset` | 세대 증가 + **비우기 신호도 큐를 태운다** |
| 전투 씬 이탈 | `Discard` | 세대 증가. 화면은 그대로 — 이미 보인 로그는 결과 화면에서도 볼 수 있다 |
| 플러그인 언로드 | `Discard` | |
| 오버레이 꺼짐(`Overlay.Enabled=false`) | — | 예약 큐에 아무것도 넣지 않는다 |

`Reset`의 비우기를 큐에 태우는 이유: 따로 보내면 메인 스레드가 이미 꺼낸 지난 판 줄이
비우기 **뒤에** 오버레이에 도착할 수 있다. 큐를 타면 FIFO라 지난 판 줄은 항상 비우기
앞에 선다.

**비우기 신호만은 세대가 지나도 버리지 않는다.** 새 판 직후 전투 이탈(`Discard`)이 겹치면
비우기의 세대가 곧바로 낡는데, 버리면 지난 판 페이지가 새 판에 남는다. 비우기는 여러 번
실행돼도 무해하다.

## 설정

`BepInEx/config/astralparty.battlelog.cfg`

| | 기본값 | |
| --- | --- | --- |
| `Overlay.SyncWithAnimation` | true | 끄면 연출 길이를 더하지 않고 받은 순서대로 표시 |
| `Overlay.SyncMaxLagMs` | 60000 | 재생 덩어리 시작 시각의 수신 대비 상한(밀리초) |
| `Overlay.ScreenSignals` | false | 주사위·PK 결과를 화면 신호로 공개(실험). [SIGNAL-GATING.md](SIGNAL-GATING.md) |
| `Diagnostics.TraceTiming` | false | 프레임·표시 진단. 켜면 F10으로 화면 시각 기록. 화면 신호 판정은 `gate` 줄로 남는다 |

측정 4에서 쓴 `Diagnostics.ScreenProbe*` 설정(오브젝트 전수 기록·경로 폴링·텍스트 후킹)은 제거했다.
기존 설정 파일에 남은 값은 무시된다. 다시 후보를 찾아야 하면 커밋 `d10b55d`의 `UI/ScreenProbe.cs`·`Plugin.cs`를 되살린다.

## 측정 현황

아래는 **이전 고정 지연 모델을 조사할 때 남긴 측정 원문**이다. 현재 순차 재생 모델의
설정법이나 검증 결과로 읽지 않는다. 측정 전에 확인한 것:

- **AnimSpeedMod 리포에 연출 시간 측정이 없다.** 기록된 측정은 설정 파일 읽기 지연
  (최악 46ms → 1.3ms) 하나뿐이다.
- **게임 코드에 쓸 만한 상수가 없다.** 연출 대기는 대부분 변수·에셋(타임라인) 값이다.
  `GachaDiceManager._DiceShowTime = 1000`은 **가챠** 주사위라 전투와 무관하다.
- **네트워크 계층에 순차 큐가 없다.** `RPCMsgManager.ReceiveRPCCallStatic`은 패킷마다
  `UniTaskExtensions.Forget(DealOnPushRPCMsg(...))`로 핸들러를 바로 띄운다. 연출 순서는
  각 게임 로직이 알아서 정한다.

### 현재 모델을 검증하는 법

`Diagnostics.TraceTiming = true`로 두고 배속 1에서 화면에 사건이 나타나는 순간 F10을
누른다. `LogOutput.log`의 `[timing] mark speed=1` 시각을 같은 사건의 `frame` 수신
시각과 `show kind=... planned=... waited=... speed=...` 표시 시각에 대응시킨다.
봇 행동이 연속되는 구간, 사용자 결정 창 앞뒤, PK 결과와 피해, 1라운드 시작을 따로
본다. 이후 배속과 방 속도를 바꿔 같은 비교를 반복한다. F10 기록은 종류별 수신 경과를
자동 계산하지 않으므로 이전 모델의 측정 예시를 그대로 적용하면 안 된다.

### 측정 1 — 타격 (2026-09-17, 기본 속도 방, AnimSpeedMod 1배속, 봇전)

PK 결과 패킷(`BattleS2C`)을 받은 뒤 **타격이 화면에 들어가기까지**. F10 기록을
`battle-log.txt`의 수신 시각과 맞춰 어느 PK인지 확인했다.

| # | PK (공격 → 방어) | 결과 수신 | 타격까지 | 그 사이 먼저 도착한 패킷 |
| --- | --- | --- | ---: | --- |
| 1 | 보니(사용자) → 마법 찻주전자1 | 23:07:30.259 | 4633ms | 없음 |
| 2 | 미사키(봇) → 마법 찻주전자3 | 23:07:48.149 | 4988ms | 골드, 펭 행동 시작, 펭 효과카드 |
| 3 | 펭(봇) → 마법 찻주전자2 | 23:07:59.156 | **7717ms** | 골드, 간수 행동 시작·주사위, 찻주전자1 행동 시작·주사위 |
| 4 | 마법 찻주전자1 → 보니 | 23:08:15.668 | 2958ms | 찻주전자2 행동 시작·주사위 |
| 5 | 마법 찻주전자2 → 보니 | 23:08:26.251 | 3458ms | 찻주전자3 행동 시작·주사위 |
| 6 | 마법 찻주전자3 → 파루난(봇) | 23:08:34.257 | 4900ms | 교도소장 행동 시작·주사위, 라운드 2 |

**중앙값 4767ms, 범위 2958~7717ms (약 2.6배).** 편차가 커서 고정 지연 하나로는 맞지 않는다.

편차의 주된 원인은 **캐릭터 스킨마다 공격 모션 길이가 달라서**다(플레이어 확인). 다만 같은
마법 찻주전자끼리도 4·5번(약 3초)과 6번(4.9초)이 달라, 방어자 쪽 모션이나 누른 시점의
오차도 섞인 것으로 보인다. 스킨별로 맞추려면 공격자 스킨을 읽어야 하는데, 지금은 `Hero`에서
`HeroId`만 읽는다(손패가 같은 메시지에 있어서). 스킨이 다른 안전한 필드에 있는지는 확인하지 않았다.

그리고 **타격이 보이기 전에 다음 차례의 패킷이 1초 간격으로 계속 도착했다**(4·5번 제외 전부
2~4개). 봇 차례는 서버 타이머로 오고 화면은 PK가 끝나야 넘어간다면, 이 줄들은 `turn`·`dice`
지연을 0으로 두는 한 PK 줄과 함께 풀려 **자기 연출보다 먼저** 보인다. 누적 모델이 필요한지는
측정 2(봇 차례 표시)로 확인한다.

원본: 이 측정의 `LogOutput.log`와 `battle-log.txt`는 리포에 넣지 않는다(참가자 표시명 포함).

**적용: `attack`·`hit` 모두 4767ms (중앙값).** 스킨 편차 때문에 절반쯤은 여전히 먼저 뜨는 것을
감수했다. PK 줄에도 같은 값을 거는 이유는, PK 줄만 먼저 뜨면 ATK·DICE로 피해량이 드러나기
때문이다(공격 시작 표본 433ms는 한 번뿐이라 쓰지 않았다). 배속을 따르는지는 아직 재지 않아
`fixed` 없이 둔다.

### 측정 2 — 봇 차례 표시 (2026-09-17, 기본 속도 방, 1배속, 프로필 없음)

`ActionStartNotifyS2C`(행동 시작)를 받은 뒤 **화면에 그 봇의 차례가 표시되기까지**.
캐릭터 봇 3명 × 2라운드 = F10 6번으로 대응시켰다(몬스터 차례는 재지 않음).

| 라운드 | 봇 | 차례 수신 | 화면 표시 | 지연 | 그 앞에 몰린 것 |
| --- | --- | --- | --- | ---: | --- |
| 1 | 파루난 | 23:13:03.189 | 23:13:06.590 | 3.40초 | 사용자(사이크스)의 이동 |
| 1 | 판다맨 | 23:13:17.194 | 23:13:27.086 | **9.89초** | 파루난 PK(추격·처치) 결과가 **같은 ms**에 도착 |
| 1 | 패드맨 | 23:13:25.198 | 23:13:43.458 | **18.26초** | 판다맨의 카드·주사위·이동·PK 전부 |
| 2 | 파루난 | 23:14:24.277 | 23:14:26.194 | 1.92초 | 사용자 이동 |
| 2 | 판다맨 | 23:14:35.284 | 23:14:40.268 | 4.98초 | 파루난 PK·체크 포인트 |
| 2 | 패드맨 | 23:14:42.288 | 23:14:45.332 | 3.04초 | 판다맨 이동 |

**1라운드에서 지연이 3.4 → 9.9 → 18.3초로 불어난다.** 해석:

- 서버는 봇 행동을 **자기 타이머(약 1초 간격)** 로 보낸다. 화면을 기다리지 않는다.
- 클라이언트는 연출을 **차례대로** 재생한다. 앞 PK가 끝나야 다음 봇 차례가 나온다.
- 그래서 봇끼리 PK가 이어지면 화면이 패킷보다 점점 뒤처진다.
- **사람이 입력하면 따라잡는다.** 사용자는 화면을 보고 주사위를 굴리므로, 그 입력이 오는
  시점에는 연출이 끝나 있다. 2라운드 사용자 주사위(23:14:18.514)는 라운드 시작 패킷보다
  25.3초 늦었고, 그 뒤 2라운드 봇 차례의 지연은 다시 2~5초로 작아졌다.

**결론: "수신 시각 + 종류별 고정 지연" 모델은 봇전에서 맞지 않는다.** 18초 지연은
`SyncMaxDelayMs`(8초)도 넘는다. 측정 1의 PK 지연(4767ms)도 봇 차례가 몰린 뒤라면 같은 이유로
짧다. 필요한 것은 앞 연출이 끝난 시점부터 다음 연출을 세는 **누적 재생 모델**과, 사람 입력을
**동기화 지점**으로 쓰는 것이다(사람 플레이어는 `Player.IsBot`으로 가를 수 있다 — 이미 읽는 필드).

### 측정 3 — 화면 지연과 내 결정 창 (2026-09-17, 봇전, 1배속, 헤더 진단 켬)

사용자는 보니, 봇은 코마치·Z3000·루루. F10은 봇 차례가 화면에 뜰 때(한 번은 실수로 간수 차례).

| 라운드 | 차례 | 수신 | 화면 | 지연 |
| --- | --- | --- | --- | ---: |
| 1 | 코마치 | 23:25:41.929 | 23:25:57.307 | 15.38초 (판 도입 연출 포함) |
| 1 | Z3000 | 23:26:00.976 | 23:26:27.597 | **26.62초** |
| 1 | 루루 | 23:26:13.983 | 23:26:43.115 | **29.13초** |
| 1 | **보니(나)** | 23:26:22.988 | 23:26:59.662 (결정 창 열림) | **36.67초** |
| 1 | 간수 | 23:27:05.342 | 23:27:07.341 | **2.00초** ← 내 입력 직후 |
| 2 | 코마치 | 23:27:18.004 | 23:27:22.760 | 4.76초 |
| 2 | Z3000 | 23:27:22.007 | 23:27:28.684 | 6.68초 |
| 2 | 루루 | 23:27:26.011 | 23:27:35.061 | 9.05초 |

(측정 2와 같은 규칙으로, F10을 차례 순서대로 대응시켰다. 간수 한 번이 수신 뒤 정확히 2.00초로
맞아 순서 대응을 뒷받침한다.)

**지연은 봇·몬스터 차례를 거치며 쌓이고, 내 결정 창이 열리면 0으로 돌아간다.**
1라운드 끝 무렵 36.7초까지 벌어졌다가, 내가 입력한 직후 간수 차례는 2.0초였다.

**내 결정 창은 헤더만으로 잡힌다.** 게임은 결정 창(카드 사용·주사위·PK 카드 등)을 열 때
`OperationTimer.ActionDownTime`이 `TimeWastingC2S(남은 시간)`을 보내고, 닫을 때 `(0)`을 보낸다.
서버 응답 `TimeWastingS2C`(5308)는 우리 요청이라 헤더 `UPSN`이 0이 아니고, 본문 길이가
열림 14바이트(시간+playerId)·닫힘 9바이트(playerId만)로 갈린다. 실측:

```
23:26:59.662 frame cmd=5308 up=42 len=14   ← 결정 창 열림 (내 차례 수신 36.7초 뒤)
23:27:01.171 frame cmd=5308 up=43 len=9    ← 닫힘
23:27:01.172 frame cmd=5056 up=44 len=9    ← 카드 사용 단계 응답
```

`5004`(Heartbeat)는 5초마다 오는 응답이라 동기화 신호가 아니다. PK에서 방어자일 때는
`BattleUseCardS2C`(5036)·`BattleChoiceS2C`(5040) 응답이 `UPSN≠0`으로 왔다(결정 창 알림은 없었다).
PK 진행 프레임(`end=0`)은 조우·양쪽 카드·주사위마다 한 번씩, 결과(`end=1`)까지 약 1초 간격이었다.

차례 사이 화면 시간(다음 차례 표시 − 이전 차례 표시)으로 본 한 차례 길이:

| 차례 | 한 일 | 길이 |
| --- | --- | ---: |
| R2 코마치 | 카드·주사위·7칸·땅 | 5.9초 |
| R2 Z3000 | 카드·주사위·8칸·체크 포인트 | 6.4초 |
| R1 Z3000 | 카드·주사위·9칸·PK·골드 | 15.5초 |
| R1 루루 | 카드·주사위·3칸·카드 제출·PK·골드 | 16.5초 |
| R1 코마치 | 카드·주사위·8칸·PK·주사위·3칸·PK·골드·이벤트 | 30.3초 |

PK가 끼면 한 차례가 약 10초 늘어난다.

### 다음 단계 — 사람 입력을 동기화 지점으로 (조사 중)

PK·판의 시작·끝 메시지는 있지만(`AskBattleS2C` 5048, `BattleS2C` `isEnd=0/1`,
`GameFinishS2C` 1016) **모두 서버 시각**이라 화면과의 격차를 줄여주지 않는다(측정 2).

화면 쪽 단서는 **사용자의 입력**이다. 주사위·카드 제출 같은 입력은 화면이 그 단계에 왔을
때만 할 수 있다. 서버가 우리 요청에 답한 프레임은 헤더의 `UPSN`이 0이 아니다(게임도 이
값으로 응답을 찾는다, `RPCMsgManager.ReceiveRPCCallStatic`). 헤더 값이라 본문 해석도,
허용목록 변경도 필요 없다.

`TraceTiming`을 켜면 이제 이런 줄이 남는다 (벽시계 시각 포함, 본문은 해석하지 않는다):

```
[timing] 23:13:03.189 frame cmd=1026 ActionStartNotify up=0 down=812 err=0 len=11
[timing] 23:13:17.194 battle id=… end=1 파루난 vs 도둑 pursuit=1 back=0
[timing] 23:13:27.086 mark speed=1 turn=1888ms …
```

(숫자는 형식 예시) 계정 uid는 남기지 않는다 — PK 줄은 표시명을 쓴다.

한 판 돌려서 볼 것: 입력 응답이 어떤 cmd로 오는지(`up≠0`), 그 시각이 화면 따라잡기와
맞는지, PK 한 번에 `end=0` 프레임이 몇 번·어떤 간격으로 오는지.

### 측정 4 — 화면 신호 프로브 (2026-09-23, 봇전 여덟 판, 완료)

당시 화면 쪽 신호는 본인 결정 창(5308)뿐이라 봇 연속 구간의 오차가 다음 결정 창까지 쌓였다.
네트워크가 아니라 **화면 오브젝트**에서 사건 경계를 직접 읽을 수 있는지 봤다. 측정 중의 프로브는
기록만 했고, 결과로 고른 주사위 눈·PK 창·타격 오브젝트만 `Overlay.ScreenSignals`로 연결했다
([SIGNAL-GATING.md](SIGNAL-GATING.md)). 아래 단계·해석 주의는 측정 당시 도구의 설명이며, 그 진단
설정은 병합 때 제거했다([설정](#설정)).

**이전 시도 (2026-09-22, 코드는 제거됨).** 같은 목적으로 두 가지를 해 봤다.

- **텍스트·이미지 후킹** — `TMP_Text`/uGUI `Text`의 텍스트 변경과 `Image`의 fill·sprite 변경을
  후킹했다. 후크는 설치됐고 호출도 잡혔지만(한 판 `calls=4957`, `err=0`), 기록된 경로는
  `AppLauncher/UGUI/Canvas/...`(런처)와 `AstralPartyKoreanPatchOverlay/...`(한글패치 모드)뿐이었다.
  전투 중 새로 기록된 UI 경로는 없었고, 전투 중 호출은 이미 본 인스턴스에 같은 값을 다시 넣는
  것이었다.
- **컴포넌트 폴링** — `TMP_Text total=0`, `Text total=15`, `Image total=9`. 그러나 조회 코드가
  플러그인 자신의 활성 `Text`도 찾지 못했으므로 **판정 불가**다. "전투 UI가 없다·폴링으로 접근할 수
  없다"는 당시 결론은 과했다.
- `GameObject.SetActive`는 시험하지 않았다.

잡힌 것이 전부 AOT 코드(런처)나 다른 모드였다는 점에서 가설이 둘 남는다. (가) 전투 UI가
TMP/uGUI 텍스트를 쓰지 않는다. (나) HybridCLR이 해석 실행하는 게임 코드의 호출은 AOT 디투어를
거치지 않는다. (나)라면 SetActive도 같은 식으로 실패한다. 그래서 `stats`에 상태 변경의 **루트
오브젝트별 개수**(`roots=`)를 싣는다 — 전투 씬에서 런처·한글패치·이 플러그인 밖의 루트가 나오면
(나)는 기각된다.

단계:

1. **후보 찾기** — `ScreenProbe = true`. `GameObject.SetActive(bool)` Postfix가 상태가 바뀐
   오브젝트의 경로를 `[probe] 시각 f=프레임 active=0|1 path=…`로 남긴다. 봇전 한 판에서 차례
   배너·주사위·PK 연출 루트처럼 사건과 1:1로 켜지는 경로를 고른다.
2. **가시 상태 폴링** — 고른 경로를 `ScreenProbeWatch`에 넣는다(세미콜론 구분).
   `activeInHierarchy`를 매 프레임 읽어 `watch=0|1 path=…`로 남긴다. SetActive 후킹이 놓치는 경우
   (Animator·Timeline이 네이티브로 켜는 것, 부모가 켜져 보이게 되는 것)도 잡힌다. 경로가
   `.../`로 시작하면(깊이 12 초과) 폴링할 수 없다.
3. **대조** — `TraceTiming`도 켜고 F10 `mark`, `frame` 수신, `segitem`과 시각을 맞춘다. 판정 기준:
   신호가 사건마다 정확히 한 번 오는지, F10과 몇 프레임 차이인지, 신호 시점에 모델 대기열에
   아직 연출 중인 줄이 남아 있는지.
4. **필요할 때만 텍스트** — 특정 숫자 UI(피해·골드)가 필요하면 그 경로 접두어를 `ScreenProbeText`에
   넣는다. 이때만 `TMP_Text`·`UI.Text` 텍스트 후킹이 설치된다. 숫자만 남기고 6자리 이상은 `#`로 가린다.

해석 주의:

- **기록 시각은 호출 시각이다.** SetActive·텍스트 setter는 그 프레임 렌더 전에 불리고, 폴링은
  `Time.deltaTime` 첫 호출 시점(프레임 초반)이라 같은 프레임의 나중 변경은 다음 프레임에 잡힌다.
  실제 표시와의 차이는 F10으로 따로 재야 한다.
- 텍스트 변경은 같은 숫자가 여러 UI에 중복될 수 있어, 경로로 좁히기 전에는 사건 대응으로 쓰지 않는다.
- **첫 중단 기준은 `stats` 줄이다.** 10초마다(호출 수가 변했을 때만)와 씬 전환 때
  `stats activeCalls= nameMatched= activeChanges= textCalls= textMatched= paths= ownSkipped= lines= budgetName= budgetPath= budgetLine= roots=루트:개수,…`를
  남긴다(`roots`는 씬마다 새로 센다). `patched`만으로는 후킹이 실제로 불리는지 알 수 없다 — 전투 씬에서 `activeCalls`가 0이면
  후킹이 안 걸린 것이고, 호출은 많은데 전투 사건에 대응하는 경로가 없으면 전투 UI가 SetActive가
  아닌 방식(Animator, CanvasGroup 알파 등)으로 바뀐다는 뜻이다. 둘을 구분해서 판정한다.
- **부하 제한은 비싼 처리 앞에 있다.** `ScreenProbeNames`로 이름을 먼저 거르고(포인터별 1회 조회),
  통과한 것만 상태 저장·경로 계산·기록을 한다. `activeCalls`(필터 전)·`nameMatched`(통과)와 단계별
  예산 초과(`budgetName` 이름 조회, `budgetPath` 경로 계산 — 둘이 프레임당 40개를 나눠 쓴다,
  `budgetLine` 기록 80줄)를 따로 세므로 누락 여부를 판정할 수 있다. 캐시된 조회는 제한 없이 통과한다.
- **경로 마스킹.** 오브젝트 이름에 닉네임이 들어갈 수 있어, ASCII 밖 글자가 섞인 이름은
  `~길이`로, 6자리 이상 숫자는 `#`로 가린다. 가린 경로는 `ScreenProbeWatch`에 쓸 수 없다.
  공유 전에는 그래도 로그를 한 번 훑어볼 것.
- 오류가 20번 나면 프로브가 스스로 꺼진다. 소켓·오버레이 기능과는 독립이다.

#### 첫 판 결과 (2026-09-23, 봇전, 1배속, 1라운드 + 2라운드 일부, F10 없음)

**가설 (나)는 기각이다.** 전투 씬 `stats`의 `roots`에 게임 쪽 루트가 나왔다. 게임 UI는
**FairyGUI**다 — `Stage`, `GComponent`, `TextField`, `GLoader`, `Shape`, `Container`, `GoWrapper`는
FairyGUI 표시 객체 이름이다. 이전 텍스트 후킹이 전투 UI를 못 잡은 이유는 가설 (가)였던 것으로
보인다(FairyGUI `TextField`는 TMP/uGUI `Text`가 아니다). 연출 쪽 오브젝트는 루트에 바로 붙은
이펙트·프리팹이라 경로가 이름 하나다.

사건과 1:1로 대응한 후보:

| 오브젝트 (`active=1`) | 대응 사건 | 근거 |
| --- | --- | --- |
| `DiceController(Clone)` + `Dice_<캐릭터>_<눈>(Clone)` | 주사위 결과가 화면에 나옴 | **눈이 이름에 있다.** 캐릭터 주사위 6번 모두 `battle-log`의 눈과 일치 (9·2·5·6·2·7) |
| `BattleShow(Clone)` | PK 연출 시작 | PK 6번 모두 |
| `BattleShow_Atk_Normal` | PK 타격 (측정 1의 "타격") | PK 5번 모두 |
| `Thinking` | 봇 차례 시작 추정 | 봇 차례 3번. 검증 필요. **사건당 1회 신호로 보지 않는다** |
| `Gold_UP` / `Gold_Down` | 골드 이동 연출 | 여러 번 깜빡여 한 사건에 여러 줄. **사건당 1회 신호로 보지 않는다** |
| `Card_Get`, `Heal_Common`, `Skill_<id>_Ready`, `Buff_<이름>` | 카드 획득·회복·스킬·버프 | 표본 적음 |

몬스터 차례의 주사위는 이 오브젝트들로 잡히지 않았다.

주사위 결과 수신 → 화면(`Dice_*`) 지연: 16.0초(판 도입 포함), 21.6, 20.5, 20.9초(봇 연속),
0.0초(사용자 본인), 5.7초(2라운드). 측정 2·3의 누적 지연과 같은 모양이다.

현재 모델의 표시(`show`)와 화면 비교 — 대체로 **늦게** 떴다:

| 사건 | 화면 | 오버레이 | 차이 |
| --- | --- | --- | ---: |
| 주사위 (1라운드 첫 봇) | 23.664 | 26.687 | +3.0초 |
| 주사위 (봇 연속 셋) | — | — | +10.2 / +9.0 / +8.2초 |
| PK 타격 (봇 연속 셋) | — | — | +3.3 / +3.7 / +2.9초 |
| PK 타격 (본인 결정 창 직후) | 55.659 | 55.538 | **−0.1초** (먼저 뜸) |
| PK 타격 (1라운드 끝) | 05.973 | 09.919 | +3.9초 |

주의:

- F10을 누르지 않아 SetActive 호출과 실제 렌더 사이의 프레임 차이는 아직 모른다.
- 전투 중 `budgetDropped`가 276 늘었다(10:16:01 전후). 그 구간은 사건이 빠졌을 수 있다. 씬 로딩
  중 9천여 건은 측정과 무관하다.
- 다른 게임을 함께 실행한 판이라 프레임·네트워크 조건이 깨끗하지 않다.
- 플레이어별 카메라 `<닉네임>_VirtualCamera`로 ASCII 닉네임이 로그에 남았다. 이후 빌드에서 가린다.
  `Dice_<캐릭터>_<눈>`도 캐릭터 번호를 지우고 `Dice_<눈>`으로 남긴다. 이 판 로그에 새 필터·마스킹을
  적용해 본 결과 기록 134줄(전에는 4,107줄), 닉네임 0건이었다.

#### 둘째 판 결과 (2026-09-23, 봇전, 1배속, 1라운드, 이름 필터, F10 3회)

닉네임은 로그 전체에서 0건이었다. `recording SetActive for …`로 필터가 걸린 것을 확인했다.

**F10과 대응 — 세 신호 모두 F10보다 0.30~0.53초 먼저 왔다.** 사람 반응 시간 범위라, F10으로는
SetActive 호출과 실제 표시 사이의 프레임 차이를 더 좁힐 수 없다. 신호 시각을 화면 시각으로 봐도 된다.

| F10 대상 | 신호 | 신호 → F10 |
| --- | --- | ---: |
| 봇 차례 | `Thinking` (켜짐과 꺼짐이 같은 프레임) | +0.45초 |
| 주사위 결과 (비전투 이동) | `Dice_Die_1` 켜짐 = `Dice_<눈>` 꺼짐 | +0.30초 |
| PK 타격 | `BattleShow_Atk_Normal` | +0.53초 |

**주사위 신호는 두 단계다.** `DiceController(Clone)`·`Dice_<눈>`이 켜지는 것은 굴리기 시작이고,
눈이 확정돼 보이는 것은 1.1~1.6초 뒤 `Dice_<눈>`이 꺼지고 `Dice_Die_1`이 켜질 때다. 두 판의 굴림
시간은 1.14·1.15·1.15·1.18초와 1.59·1.59·1.59초로 두 무리로 갈렸다(캐릭터 주사위 종류 차이로 추정).

현재 모델과 비교:

| 사건 | 화면 | 오버레이 | 차이 |
| --- | --- | --- | ---: |
| 첫 봇 차례 | 17.163 | 27.783 | +10.6초 |
| 첫 봇 주사위 결과 | 20.771 | 30.650 | +9.9초 |
| 둘째·셋째 봇 주사위 결과 | 27.594 / 29.894 | 32.120 (결정 창 동기화로 방출) | +4.5 / +2.2초 |
| 본인 주사위 결과 | 40.503 | 39.360 | **−1.1초** |
| PK 타격 (본인 결정 창 직후) | 54.930 | 52.543 | **−2.4초** |

- 판 도입: 1라운드 수신 → 첫 `Thinking`이 이 판 9.5초, 첫 판 14.6초였다. 모델의 18.15초는 둘 다보다
  길어 첫 봇 차례 전체가 10초 가까이 늦게 뜬다.
- 본인 차례 직후는 반대로 **먼저 뜬다**. 주사위 줄은 굴리기 시작에 나가 결과보다 1.1초 이르고, PK
  결과는 수신 뒤 1.94초 공개로는 타격(수신 뒤 4.33초, 첫 판 3.61초)보다 이르다.

주의:

- 이 판에서 `budgetName`이 12,944였다(전체 호출 17,352). 이름 조회가 경로 계산과 프레임당 40개
  예산을 나눠 써서, FairyGUI가 한 프레임에 많은 오브젝트를 켜고 끌 때 대상 오브젝트도 버려질 수
  있었다. 이름 조회 예산을 따로 1000으로 분리했다. 이 판의 신호는 누락이 있을 수 있다.
- 판이 짧아 PK는 한 번뿐이었다. `BattleShow(Clone)`는 그 한 번에 켜졌다.
- 판 종료 때의 `Il2CppInterop` 오류(닫힌 소켓의 `EndReceive`)는 기존 소켓 후킹 설계상 드러나는
  게임 예외로, 프로브와 무관하다.

#### 셋째 판 결과 (2026-09-23, 봇전, 1배속, 2라운드, 이름 예산 분리, F10 없음)

닉네임 0건. `budgetName` 1,890은 전부 전투 씬 진입 전 로딩 구간이고 **전투 중 예산 초과는 0**이었다.

**주사위 — 캐릭터 주사위 9번 모두 눈까지 순서대로 일치.** 몬스터 주사위는 이번에도 신호가 없다.

**PK — 8번 중 `BattleShow(Clone)` 8번, `BattleShow_Atk_Normal` 7번.** 빠진 한 번은 적을 처치한 PK
(HP 7→0)였고, 그 PK에서는 `BattleShow`로 시작하는 다른 이름도 켜지지 않았다. 처치 연출이나 캐릭터별
공격 연출은 다른 오브젝트를 쓰는 것으로 보인다. 한 캐릭터의 PK 두 번은 `BattleShow_Atk_Normal`이
0.2초 안에 세 번 켜졌다(연타 연출 추정) — 첫 번째를 타격 시각으로 본다. `BattleShow(Clone)` →
첫 타격은 5.07~6.56초.

**`Thinking`은 차례 신호로 쓸 수 없다.** 봇마다 별도 오브젝트가 첫 차례에 켜진 뒤 판 끝까지 켜진 채라,
상태 변화로는 봇의 첫 차례만 잡힌다(2라운드 봇 차례 3번 모두 신호 없음).

현재 모델과 비교 (화면 = `Dice_Die_1`·첫 `BattleShow_Atk_Normal`):

| 구간 | 주사위 | PK 결과 |
| --- | --- | --- |
| 봇 차례 | +2.8 / +5.0 / +3.5 / +5.5 / +4.4 / +6.4초 (늦음) | +2.3 / +3.6 / +3.5 / +2.2 / +4.9 / +2.1초 (늦음) |
| 본인 결정 창 직후 | **−1.1 / −1.3 / −1.1초** (먼저 뜸) | **−1.3초** (먼저 뜸) |

판 도입은 1라운드 수신 → 첫 `Thinking` 13.5초였다(앞 판들 9.5·14.6초, 모델 18.15초).

세 판을 합치면 방향이 일정하다. **봇 차례는 2~6초(첫 차례는 10초 안팎) 늦고, 본인 결정 창 직후는
주사위 약 1.1~1.3초, PK 결과 1.3~2.4초 먼저 뜬다.** 먼저 뜨는 쪽은 결과를 미리 드러내므로 더 나쁘다.

#### 넷째 판 결과 (2026-09-23, 봇전, 1배속, 4라운드, 공개 하한 적용 빌드, F10 없음)

다른 맵이다(원숭이 깜짝 상자·보스·톱날 원숭이 조수, 땅 "재앙"). 닉네임 0건, 전투 중 예산 초과 0건.

**새 타격 오브젝트:** `BattleShow_Atk_High`, `BattleShow_Atk_High_Layer1`. 접두어 `BattleShow`에 이미
걸린다. 셋째 판의 "처치 PK 타격 누락"은 이런 다른 이름이었을 가능성이 있다.

사건별 대조 (`segitem` idle → 사이의 synclead → show → 화면 신호):

| 구분 | 결과 |
| --- | --- |
| 본인 주사위 4건 (idle=1, 모두 사이에 5308/9 동기화) | **4건 모두 +0.45초 늦음.** 공개 하한이 동기화를 이겼다(이전 빌드 −1.1초) |
| 쉬는 화면 PK 3건 (idle=1, 1건은 사이에 5040/14) | +0.98 / +1.54 / +1.06초 늦음 |
| 봇 주사위 (idle=0) 1~3라운드 9건 | 늦음 7건(+0.28~+6.76초), **먼저 2건(−1.71, −1.33초)** |
| 4라운드 초 봇 주사위 3건 (idle=0) | **먼저 −6.48 / −8.15 / −10.10초** |
| 본인 두 번째 주사위 (PK 직후, idle=0) | **먼저 −1.23초** |
| 밀린 PK 5건 (idle=0) | **먼저 −0.83 / −0.32 / −0.79 / −1.81 / −9.83초** (넷은 처치 PK) |
| 4라운드 PK 1건 (idle=1) | **먼저 −8.95초** — 모델은 쉰다고 봤지만 화면은 밀려 있었다 |
| 한 캐릭터의 PK 2건 | 타격 신호 없음 → **판정 불가** (`BattleShow(Clone)`만 켜짐) |
| 몬스터 주사위 4건 | 화면 신호 없음 → **판정 불가** |

해석:

- **이번 수정(공개 하한)은 겨냥한 곳에서 동작했다.** 본인 주사위·쉬는 화면 PK는 모두 늦는 쪽이 됐다.
- **밀린 쪽은 이 맵에서 먼저 뜬다.** 앞 세 판에서 봇 차례가 늦었던 것은 커서가 화면보다 앞서 있어서였을
  뿐, 밀린 PK의 공개 1.94초가 맞아서가 아니었다. `BattleShow(Clone)` → 첫 타격은 5.07~7.3초인데,
  커서가 화면 PK 시작과 비슷하면 1.94초로는 먼저 뜬다.
- **4라운드 초의 큰 오차는 모델이 모르는 연출 때문이다.** 10:57:32 동기화 뒤 보스·몬스터 세 차례(주사위
  10·2·5칸)와 라운드 전환을 모델은 주사위 2.70초씩으로만 셌고, 화면은 약 5.3초 더 걸렸다. 몬스터 이동
  17칸 × 0.30초 ≈ 5.1초와 비슷해 **몬스터 이동은 칸 수에 비례할 가능성**이 있다(캐릭터 주사위는 비례하지
  않았다). 표본 하나라 가설이다. 이 외에도 쓰러짐(HP→0)·땅 "재앙" 같은 연출은 길이가 0이다.
- 이 오차 때문에 쉬는 화면 판정(`idle=1`) 자체가 틀릴 수 있다. 공개 하한은 수신 기준이라 이런 경우를
  막지 못한다.

#### 다섯째 판 결과 (2026-09-23, 봇전, 1배속, 8라운드, 넷째 판과 같은 빌드, F10 없음)

닉네임 0건, 전투 중 예산 초과 0건. 캐릭터 주사위 28건 모두 눈이 맞았고, PK 21건 모두 창에 대응했다.

- **반격은 원래 PK와 같은 창을 쓴다.** 창 18개 = PK 21 − 반격 3. 반격의 타격은 원래 타격 오브젝트가 꺼진
  뒤 6.8~7.1초에 새로 켜진다. 연타(셋째·넷째 판)는 인스턴스가 겹쳐 켜진다. 설계에 "켜진 인스턴스 수가
  0→1이 되는 순간" 규칙으로 반영했다.
- 타격 신호 없음 3건(파파라 2, 파루난의 처치 추격 1). 다섯 판 합계 6/41이고, 모두 특정 캐릭터다.
- 창 밖에서 켜진 타격 0건. 결과 수신과 화면 신호 사이의 결정 창 열림 0건(주사위 0/28, PK 0/21).
- 현재 모델은 이번에도 캐릭터 주사위 9건을 먼저 띄웠다(−0.2~−3.6초, `idle=1` 2건 포함).
- **몬스터 이동 가설은 판정하지 못했다.** 몬스터 차례는 항상 라운드 끝에 PK·라운드 전환·결정 창과 섞여
  있어, 세 판(셋째~다섯째)에서 몬스터 차례만 있는 구간이 하나도 없었다.

#### 여섯째 판 결과 (2026-09-23, 봇전, 1배속, 3라운드, `ScreenSignals = true` 첫 실게임)

닉네임 0건, 전투 중 예산 초과 0건. 후킹 실패 경고 없음.

- **판정 가능한 결과 줄 20건 중 화면보다 먼저 뜬 것 0건.** 캐릭터 주사위 14건은 `Dice_<눈>` 꺼짐과 같은
  프레임(+0.02~0.04초, 분석 기준을 `Dice_Die_1` 켜짐으로 잡은 1건은 −0.01초 = 한 프레임)에 공개됐다.
  PK 6건은 모두 첫 타격(`Atk_High` 4, `Atk_Normal` 2)과 같은 프레임에 공개됐다.
- `gate` 기록: 신호 공개 20(주사위 14, PK 6), 늦은 경로 7(주사위), `timeout`·`weak`·`extra-strike`·`consume` 0,
  `stray` 2. 이번 판에는 반격이 없어 반격 규칙은 실게임에서 확인하지 못했다.
- **`stray` 2건은 카드로 굴린 주사위 두 개("5+2")다.** 화면에는 `Dice_5`·`Dice_2`가 함께 켜지고 꺼졌지만,
  로거는 두 개 주사위의 눈을 0으로 넘겨 짝이 없었다. 그 줄은 다음 캐릭터 주사위 신호까지 기다려 화면보다
  약 15초 늦게 공개됐다.
- **몬스터 차례에서 크게 늦는다.** 몬스터 주사위는 신호가 없어 다음 캐릭터 신호까지 기다린다. 1라운드
  끝 몬스터 주사위 3건은 수신 뒤 40~44초에 공개됐고, 그 뒤 줄도 같이 멈췄다가 다음 캐릭터 주사위 신호에
  한꺼번에 나왔다.
- 신호를 받은 줄도 앞의 일반 줄이 모델 연출 길이를 기다리면 함께 기다린다(스킬 1.92초 → +1.94초 1건).

#### 일곱째 판 결과 (2026-09-23, 봇전, 1배속, 7라운드, 두 개 주사위 보완 빌드)

닉네임 0건, 전투 중 예산 초과 0건. **먼저 뜬 결과 줄 0건**(캐릭터 주사위 28, PK 18). 반격 없음.

- **두 개 주사위 보완 확인.** 자스민 "1+9"가 `Dice_1`·`Dice_9` 꺼짐과 같은 프레임에 공개됐다. `stray` 0건.
- **PK 장부 연쇄.** 타격 신호가 없는 파파라의 PK 2건이 약한 창으로 끝난 뒤 다음 캐릭터 주사위 신호로 늦게
  공개되며 장부에 올랐다. 이후 다른 PK의 실제 타격이 그 장부 항목을 먼저 소비해(`consume` 11), 그 PK도 늦은
  경로로 공개되고 다시 장부에 오르는 일이 판 끝까지 이어졌다. PK 18건 중 신호 공개 3, 늦은 공개 15이고,
  연쇄 뒤 PK는 화면보다 4~42초 늦었다. 설계의 "장부가 틀린 경우 한 칸씩 밀려 늦게 뜬다"가 실제로 났고,
  타격 없는 캐릭터가 있으면 한 번으로 판 끝까지 간다.
- **보완:** 타격 없이 닫힌 창이 자기 PK를 정산하게 했다([SIGNAL-GATING.md](SIGNAL-GATING.md) 1절 PK). 이 판 로그를
  새 규칙으로 재생하면 타격 있는 PK 14건은 타격과 같은 순간에, 파파라 PK 4건은 다음 신호까지 +3.7~21.3초 늦게
  공개되고 먼저 뜬 것은 0건이다. 여덟째 판에서 실게임으로 확인했다.

#### 여덟째 판 결과 (2026-09-23, 봇전, 1배속, 5라운드, 약한 창 정산 빌드)

닉네임 0건, 전투 중 예산 초과 0건. **먼저 뜬 결과 줄 0건**(캐릭터 주사위 23, PK 14). 반격 없음.

- **PK 장부 연쇄 해소.** PK 14건 중 13건이 타격과 같은 순간에 공개됐다. 타격 없는 파파라 PK 1건은 약한 창에
  정산되고(`weak settle=pending`) 다음 주사위 신호에 +8.2초로 공개됐다. `consume`·`weak-orphan`·`stray` 0건.
- **주사위 여러 개 4건 모두 신호로 공개**(6+1, 3+8, 10+10, 6+7). 같은 눈 두 개(10+10)도 두 오브젝트가 따로
  꺼져 짝이 맞았다. 모두 마지막 눈 꺼짐 뒤 +0.002~0.006초.
- **남은 지연:** 신호를 받은 주사위가 앞의 일반 줄(카드) 모델 대기 2.87초 뒤에 나온 경우 2건(+3.48, +2.88초).
  신호는 화면이 앞 연출을 이미 지났다는 뜻이므로 이 대기는 불필요하다.

화면 신호 연결의 규칙과 남은 검증 범위는 [SIGNAL-GATING.md](SIGNAL-GATING.md)에 정리했다.

## 알려진 한계

- 연출 길이는 관측 20개로 맞춘 값이다. 다른 캐릭터 모션, 배속, PvP에서의 오차는 확인하지
  않았다. 측정 1~3은 학습 자료이므로 별도 판으로 다시 검증해야 한다.
- `ScreenSignals`를 켜면 몬스터 주사위는 화면 신호가 없어 다음 캐릭터 신호까지 늦게 뜬다(여섯째 판
  40~44초). 먼저 뜨지는 않는다. 이번 범위에서는 고치지 않은 확인된 한계다.
- PK 결과와 피해 사이에 다른 출력 줄이 끼면 FIFO 출력 순서 때문에 피해 줄이 PK 결과와
  동시에 나오지 않는다. 측정 3의 PK 결과 다섯 건은 다음 프레임이 모두 능력치 갱신이었다.
- 동기화는 실측한 세 응답에 한정한다. 다른 입력 단계에서는 화면을 따라잡았어도
  재생 커서가 늦게 갱신될 수 있다.
- `SyncMaxLagMs`는 덩어리의 **시작**만 제한한다. PK 공개 지연과 FIFO 대기까지 포함한
  각 줄의 최종 표시 지연을 보장하지 않는다.
- 게임이 `timeScale`을 1보다 작게 낮추는 연출이 있다면 수신 시점의 배속으로 예약하므로
  이후 배속 변화에 맞춰 재계산되지 않는다.
- 게임이 `timeScale = 0`으로 멈추는 일이 있으면 프레임 시계는 계속 흐르므로 그동안
  줄이 먼저 풀린다. 온라인 게임이라 없을 것으로 보지만 확인하지 않았다.
- **게임 자체의 방 속도 설정은 반영하지 않는다.** 게임은 `timeScale`을 쓰지 않고
  `Room.SpeedType`(필드 40)으로 설정표(`ChoosingTimeLimit` gamespeed)를 찾아
  `AnimSpeed`/`DiceSpeed`/`PerformSpeed`/`VfxSpeed`를 분야별로 따로 적용한다
  (`GameLogic/BattleConfig`). 빠른 방에서는 연출이 빨라지는데 여기서는 1배속으로 계산하므로
  **로그가 연출보다 늦게** 뜰 수 있다. 필요해지면 위 필드와 설정표를 읽어 종류별 배율로
  곱해야 한다. 현재 연출 길이는 기본 속도 방 기준이다.
  게임 코드에서 `Time.timeScale`을 직접 바꾸는 곳은 개발자용 `UI/GMWindow` 하나뿐이다.
- 애니메이터 속도만 바꾸고 `timeScale`은 두는 다른 배속 모드도 감지하지 못한다.

## 검증 상태

| | |
| --- | --- |
| 순차 재생 하네스 (`tools/harness`) | 통과, 실패 0 — 순서, 동기화 신호 대상(5308·5036·5040만), 지연 상한, PK 결과·피해, 숨긴 카드, 배속, 판 시작, 새 판 초기화, 쉬는 화면·밀린 덩어리의 공개 지연 구분과 동기화·조금 밀린 PK의 공개 하한(P18~P22). PK 중간 출력 사례는 한계로 확인 |
| 화면 신호 하네스 | 통과, 실패 0 — [SIGNAL-GATING.md](SIGNAL-GATING.md) 6절 항목(G1~G21, W1~W6, L1~L2). G21은 실행 중 신호가 끊겼을 때 모델 예약으로 넘기는 경로 |
| 빌드 | Release 경고 0개, 오류 0개 |
| 실게임 | 모델 경로: 봇전 측정 1~4. 화면 신호: 봇전 세 판(여섯째~여덟째)에서 먼저 뜬 결과 줄 0건. **PvP는 로그가 없어 미검증**이고, 반격은 실게임 표본이 없어 기록 재생으로만, 배속은 확인하지 않았다 |
