# Astral Party Battle Log

[![Latest release](https://img.shields.io/github/v/release/YoungHoon02/astral-party-battle-log?sort=semver&style=for-the-badge&logo=github&label=download)](../../releases)

Astral Party의 전투 흐름을 라운드별로 정리해 주는 BepInEx 플러그인입니다.
게임 안에서는 오버레이로 확인할 수 있고, 같은 내용이 `battle-log.txt`에도 저장됩니다.

```
──────── Round 2 ────────
· 낸시 루 행동 시작
[R2] 낸시 루 스킬 사용 "원격 침입"
[R2] 낸시 루 주사위 6 → 6칸
[R2] 낸시 루 효과카드 "왕의 힘"
[R2] 낸시 루 (카드 "왕의 힘")
        낸시 루 HP 9→5/9  -4
        낸시 루 ATK → 10
[R2] PK  낸시 루 ATK5 DICE5  vs  마법 찻주전자3 DEF1 DICE4
        마법 찻주전자3 HP 10→6/10  -4
[R2] 파루난 → 낸시 루 5골드 (상점 구매)  [13→8, 5→10]
```

- 라운드 하나가 한 페이지입니다. 커서를 창에 올리고 **마우스 휠**로 스크롤합니다.
- 창 왼쪽 위 **점 여섯 개 손잡이**를 누른 채 끌면 위치를 바꿀 수 있습니다. 놓은 자리는
  다음 실행에도 유지되며, 해상도가 달라져도 창이 화면 밖으로 벗어나지 않게 조정됩니다.
- 창 크기는 `FontSize` × `Lines`로 고정됩니다(가로 폭은 `FontSize`에 비례해 자동으로
  정해집니다). 로그 양에 따라 커지거나 줄어들지 않으며, 창보다 긴 줄은 창 경계에서
  잘립니다.
- **F9**로 켜고 끕니다. 단축키는 설정에서 변경할 수 있습니다.
- 픽창이 끝나고 전투 화면으로 넘어가면 저절로 뜨고, 판이 끝나 전투 화면을 나가면
  숨습니다. 로비나 지난 판에서 F9로 껐더라도 새 판에서는 다시 표시됩니다.

## v0.3.0

- 주사위 결과와 PK 결과를 게임 화면의 주사위 눈·타격 장면에 맞춰 보여줍니다.
- 별도의 타격 신호가 없는 PK는 해당 연출이 끝나는 순간 결과를 보여줍니다.
- 앞 카드·스킬 기록의 추정 시간이 남아 있어도 화면이 뒤 사건에 도달하면 대기 중인 기록을 순서대로 표시합니다.

## v0.2.0

- 판 도중 재접속하거나 게임을 다시 켜도 같은 판의 참가자 이름을 복원합니다. 보관 파일에는
  계정 ID를 그대로 남기지 않으며, 새 판에 들어가거나 전투를 나가면 자동으로 지웁니다.
- 연결이 끊긴 뒤 다시 접속했을 때 이전 연결의 수신 데이터가 남아 참가자 명단 등의 기록이
  빠질 수 있던 문제를 해결했습니다.
- 주사위 결과가 게임 연출보다 먼저 표시되는 문제를 줄이고, 재접속으로 현재 라운드를 알 수
  없을 때 잘못된 `R0` 대신 `R?`로 표시합니다.

## v0.1.9

- 쓰러진 대상에게서 한꺼번에 해제된 버프를 한 줄로 묶고, 게임 종료 표시를 정리 기록 뒤에 둡니다.
- 새로 등장한 맵 몬스터의 이름과 원인 없는 효과의 대상, 스택 이름, 레벨 상승을 더 정확하게 표시합니다.
- 실제 글꼴의 줄 높이를 기준으로 로그창 아래쪽의 빈 공간을 줄입니다.
- 배포 ZIP에 사용 전 안내, 설치 방법, 라이선스를 함께 고지합니다.

## v0.1.8

- 긴 스킬 PK 기록이 오버레이 오른쪽에서 잘리던 문제를 줄이도록 로그창 너비를 넓혔습니다.

## v0.1.7

- 글자 크기를 바꾸면 로그창 너비도 함께 조절됩니다. 별도의 `Overlay.Width` 설정은 더 이상
  사용하지 않으며, 기존 설정 파일에 남은 값은 무시됩니다.

## v0.1.6

- 로그창을 고정 크기로 바꿔 로그가 늘거나 줄어도 창이 움직이지 않게 했습니다. 새 설치의
  기본 폭은 460이며, 기존 설치의 폭 설정값은 유지됩니다. 긴 줄은 창 경계에서 잘립니다.
- 행동이 이어질 때 오버레이가 연출 길이를 순서대로 계산해 결과가 화면보다 너무 일찍
  나타나는 문제를 줄였습니다. 사용자 결정 단계의 응답을 받으면 표시 시점을 다시 맞춥니다.
  파일 로그에는 수신 즉시 기록됩니다.

## v0.1.5

- 게임을 끄지 않고 매칭으로 연속 플레이할 때 지난 판의 참가자·라운드·몹 번호가 이어져
  `린2`, `[R5]`처럼 표시되던 문제를 해결했습니다. 사용자 방뿐 아니라 매칭과 싱글
  플레이도 새 판으로 올바르게 구분합니다.
- 실패한 판 시작 응답은 무시하고, 시작 신호를 놓치더라도 방이 바뀌면 한 번만
  초기화하도록 보완했습니다.
- 고해상도에서 작은 로그창이 화면 오른쪽·아래쪽까지 이동하지 못하던
  문제를 해결했습니다.
- `Round` 제목과 전투 로그 사이의 불필요한 빈 공간을 줄였습니다.

게임을 끄지 않고 매칭 두 판을 이어서 진행한 뒤 `LogOutput.log`를 확인하면 됩니다. 두 번째
판 앞에 `[game] new game (MatchSuccess)`가 한 번 나오고 참가자 이름·라운드·몬스터 번호가
새로 시작해야 합니다. `room changed without a start signal`이 나오면 방 변경 안전망이
작동한 것입니다. 이 안전망이 이미 전투 화면에 들어온 뒤 작동하면 오버레이는 다음
라운드에서 표시될 수 있습니다.

이전 버전의 변경 내용은 [Releases](../../releases)에서 확인할 수 있습니다.

## 주요 기능

- 로그창 왼쪽 위의 점 여섯 개 손잡이로 창을 옮길 수 있습니다.
- 옮긴 위치는 설정 파일에 저장되어 다음 실행에도 유지됩니다.
- 해상도나 화면 비율이 바뀌어도 창과 손잡이가 화면 밖으로 사라지지 않게 조정됩니다.
- 픽창에서는 로그창을 숨기고 전투 화면으로 넘어간 뒤 자동으로 표시합니다.
- `칩 획득`은 게임에서 칩 선택이 확정된 시점을 기준으로 기록합니다.
- 이미 가진 칩의 효과는 `(칩 "이름")`으로 표시하고, 정보가 없는 `칩 갱신`은 기록하지 않습니다.
- 같은 스킬 사용이나 같은 대상에 대한 기록이 불필요하게 반복되는 경우를 줄였습니다.
- 최대 HP에서 회복을 받아 실제 HP가 변하지 않은 줄은 기록하지 않습니다.
- 참가자 목록은 캐릭터가 확정된 뒤에만 기록해 계정 닉네임이나 숫자 ID가 먼저 노출되지
  않도록 합니다.

```
[R2] 미미 칩 획득 "완충 방패"
[R3] 미미 (칩 "완충 방패")
        치유 미미 0→2 (+2)
```

로그 정확도 검증 결과와 아직 확정하지 못한 항목은
[기술 문서의 v0.1.3 수정 후 실측](docs/ARCHITECTURE.md#v013-수정-후-실측)에 정리했습니다.

## 설치

1. 게임에 [BepInEx 6 (IL2CPP)](https://github.com/BepInEx/BepInEx)를 설치하고 한 번 실행합니다.
2. [Releases](../../releases)에서 최신 `AstralPartyBattleLog-v*.zip`을 받습니다.
3. ZIP 안의 `BepInEx` 폴더를 게임 폴더(`Astral Party/8vJXnINT/`)에 덮어씁니다.
4. 게임을 실행합니다.

이미 다른 모드로 BepInEx 6 IL2CPP가 설치되어 있다면 BepInEx를 다시 설치할 필요가 없습니다.
이 경우 최신 플러그인 ZIP의 내용을 게임 실행 파일(`AstralParty_INT.exe`)이 있는 게임 폴더에
그대로 풀어 주세요. `BepInEx` 폴더가 `BepInEx/BepInEx`처럼 한 단계 더 중첩되면 플러그인이
로드되지 않으므로, 게임 폴더 바로 아래에 `BepInEx` 폴더가 있어야 합니다. 처음 설치한
BepInEx는 게임을 한 번 실행해야 필요한 파일과 설정을 생성합니다.

**카드/스킬 이름표(`names.tsv`)는 따로 받지 않아도 됩니다.** 플러그인이 게임에 처음
들어갈 때 게임 설정에서 직접 만듭니다. 만들어지기 전까지는 이름 대신 ID가 보입니다.

제거하려면 게임을 종료한 뒤 `BepInEx/plugins/AstralPartyBattleLog/` 폴더를 삭제합니다.

## 알아둘 것

파일 로그는 서버에서 전투 결과를 받는 즉시 기록하므로 게임 애니메이션보다 먼저 남을 수 있습니다.
PvP에서는 작은 정보 우위가 될 수 있으므로 사용 전에 이 점과 게임 이용약관을 확인해 주세요.

오버레이는 주사위·PK 결과 줄을 게임 화면에 주사위 눈이나 PK 타격이 나타나는 순간에 맞춰
보여줍니다. 그 밖의 줄은 연출 길이를 누적해 화면 표시 시점을 추정하므로 먼저 또는 늦게 표시될 수
있습니다. 타격 신호가 없는 PK는 해당 연출이 끝날 때 표시합니다. 몬스터 주사위처럼 화면 신호가 없는
결과는 다음 화면 신호까지 늦게 표시될 수 있습니다. 자세한 내용은 [오버레이 연출 동기화](docs/OVERLAY-TIMING.md)를
참고하세요.

**상대 손패는 읽지 않습니다.** 손패 정보를 처리하는 기능 자체가 포함되어 있지
않습니다. 읽는 정보의 범위와 기술적인 근거는
[기여 안내](CONTRIBUTING.md#카드에-그은-선)에 정리되어 있습니다.

기록 대상은 보드에 공개적으로 사용한 효과 카드, PK에 카드를 냈다는 사실, HP·공격·
방어·버프 변화, 주사위, 칩과 골드입니다. PK에 낸 카드의 종류는 기록하지 않습니다.

**계정 식별자는 기본 로그에 남기지 않습니다.** 로그를 공유할 때 다른 사람의 계정
정보가 함께 전달되지 않도록 캐릭터 이름만 사용합니다. 진단이 필요한 경우에만
`LogPlayerIds` 설정으로 표시할 수 있습니다.

## 설정

`BepInEx/config/astralparty.battlelog.cfg` — 게임을 한 번 실행하면 만들어집니다.

| | 기본값 | |
| --- | --- | --- |
| `Overlay.Enabled` | true | 인게임 오버레이 |
| `Overlay.Lines` | 14 | 창 높이(보이는 줄 수) |
| `Overlay.FontSize` | 15 | 글자 크기. 창 가로 폭도 이 값에 비례해 같이 조절됩니다 |
| `Overlay.ToggleKey` | F9 | 켜고 끄는 키 |
| `Overlay.ScrollLines` | 3 | 휠 한 칸에 움직일 줄 수 |
| `Overlay.PositionX` | -1 | 창 왼쪽 위 모서리의 가로 위치 (화면 높이 1080 기준 좌표). 손잡이를 끌면 저장됩니다 |
| `Overlay.PositionY` | -1 | 창 왼쪽 위 모서리의 세로 위치 (위에서부터). -1이거나 값을 지우면 화면 왼쪽 아래 기본 위치 |
| `Overlay.SyncWithAnimation` | true | 오버레이 줄을 게임 연출에 맞춰 표시합니다. 주사위·PK 결과는 화면의 주사위 눈·타격에, 나머지는 연출 길이 추정에 맞춥니다. 끄면 받는 즉시 표시합니다. 파일 로그는 그대로입니다 |
| `Overlay.SyncMaxLagMs` | 60000 | 화면 재생 시작 시각의 수신 대비 최대 지연(밀리초) |
| `Diagnostics.TraceTiming` | false | 표시 시점 진단 기록. 켜면 F10으로 화면에서 본 시각을 남깁니다 |
| `Output.WriteFile` | true | `battle-log.txt`에도 남기기 |
| `Output.LogCards` | true | 공개된 카드 기록 |
| `Names.Rebuild` | false | 이름표를 다시 만듭니다 (게임 업데이트 후) |

## 개발 문서

| 문서 | 용도 |
| --- | --- |
| [기여 안내](CONTRIBUTING.md) | 정보 수집 범위, 개인정보 보호 기준, PR 체크리스트 |
| [아키텍처 및 제약](docs/ARCHITECTURE.md) | 현재 구현 구조와 반드시 지켜야 할 기술 제약 |
| [프로토콜 필드](docs/protocol-fields.md) | 디코딩하는 메시지와 필드 배치 |
| [오버레이 연출 동기화](docs/OVERLAY-TIMING.md) | 표시 지연 구조와 측정 절차 |
| [화면 신호로 결과 공개](docs/SIGNAL-GATING.md) | 주사위·PK 결과를 화면 신호에 맞추는 규칙, 검증 범위, 남은 한계 |
| [조사 기록](docs/FINDINGS.md) | 구현 전에 확인한 게임 구조와 근거 |
| [초기 설계 기록](docs/LOGGER-DESIGN.md) | 현재 구현과 다를 수 있는 초기 설계 결정 |

## 소스에서 빌드

.NET SDK 6 이상이 필요합니다.

```bash
git clone <this repo>
cd AstralPartyBattleLog
dotnet build AstralPartyBattleLog.csproj -c Release
```

빌드하기 전에 `.csproj`의 `<BepInExDir>`를 자신의 게임 설치 경로로 변경합니다.

빌드 결과는 `bin/Release/net6.0/AstralPartyBattleLog.dll`에 생성됩니다.

PowerShell에서 `./tools/build-release.ps1`로 배포 ZIP을 만듭니다.
이 스크립트는 DLL과 사용 전 안내, 설치 방법, 라이선스를 플러그인 폴더에 함께 넣습니다.

개발 배경과 제약은 [아키텍처 및 제약](docs/ARCHITECTURE.md)에 설명되어 있습니다.
기여하기 전에는 정보 수집 범위와 개인정보 보호 기준이 담긴
[기여 안내](CONTRIBUTING.md)를 먼저 확인해 주세요.

## 크레딧

인게임 오버레이 구현 방식은 **[astral-party-korean-patch](https://github.com/maynut02/astral-party-korean-patch)**
의 `OverlayUi`를 참조했습니다 (작성자 허락을 받았습니다). IMGUI 대신 uGUI를 사용하고 게임 폰트를
그대로 가져오는 접근도 해당 프로젝트에서 참고했습니다.

## 라이선스

[MIT](LICENSE) — **이 리포의 코드에만** 적용됩니다.

Astral Party 자체와 그 자산·프로토콜·거기서 파생된 데이터(카드/스킬/캐릭터 이름,
설정 테이블)는 각 권리자의 것이고, 그런 파일은 이 리포에 넣지 않았습니다.
`names.tsv`가 커밋되어 있지 않은 이유도 그 때문입니다 — 플러그인이 각자의 게임에서
직접 만듭니다.
