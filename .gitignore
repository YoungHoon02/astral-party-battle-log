bin/
obj/
__pycache__/

# 개인 에이전트 설정. 기술 문서는 docs/ 와 CONTRIBUTING.md 에 있다.
# 로컬 게임 경로 같은 이 기계에만 해당하는 것이 들어가므로 커밋하지 않는다.
.claude/
.agents/
AGENTS.md

# ── 게임에서 나온 것은 커밋하지 않는다 ─────────────────────────────────
#
# 전부 게임사(그리고 한글패치의 경우 번역자)의 저작물이다. 이 리포의 MIT는
# 여기 있는 코드에만 적용되며, 게임 자산을 재배포할 권리를 주지 않는다.

# 추출한 게임 어셈블리. 분석 참고용으로 로컬에만 둔다.
ref/

# 게임 텍스트 + 한글패치 번역문. 플러그인이 첫 실행에 스스로 만든다
# (Log/NameHarvest.cs). 오프라인 생성은 tools/extract_names.py.
names.tsv

# 게임 어셈블리의 전체 타입 목록 — ref/와 같은 성격이다.
docs/types-all.txt

# 진단 산출물. 로컬에서만 본다 (docs/TIMING-REPLAY-HANDOFF.md 3절).
replay/
replay-*.jsonl
